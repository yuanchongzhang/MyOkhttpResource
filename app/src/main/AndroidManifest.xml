<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhangyu.myophttpopen">


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:name=".GApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="design_width"
            android:value="1080" />
        <meta-data
            android:name="design_height"
            android:value="1920" />

        <activity android:name=".MyDemoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.BannerActivity" />
        <activity android:name=".activity.JsonCallBackActivity" />
        <activity android:name=".activity.ImitationWxActivity"></activity>

        <activity android:name=".activity.MainActivity_P2P" />
        <activity android:name=".activity.MaterialDesignActivity" />
        <activity android:name=".activity.MyToolBarActivity" />
        <activity android:name=".activity.RecycleViewActivity" />
        <activity android:name=".activity.ResultActivity" />
        <activity android:name=".activity.ScreenActivity" />
        <activity android:name=".activity.TableLayoutActivity" />
        <activity android:name=".activity.NewMainActivity" />
        <activity android:name=".activity.MySectionActivity" />
        <activity android:name=".activity.HeadActivity" />
        <activity android:name=".activity.ClipImageActivity"/>
    </application>

</manifest>